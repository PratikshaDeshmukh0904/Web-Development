function Merge(farray,Sarray){
    for(element of farray)
    {
        console.log(element);
    }

    for(element of Sarray)
    {
        console.log(element);
    }
}
let f=[1,2,3];
let s=[5,6,7];
Merge(f,s);